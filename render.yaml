services:
  - type: web
    name: vscode-server
    runtime: docker
    plan: free
    dockerfilePath: Dockerfile.render
    envVars:
      - key: PASSWORD
        sync: false
      - key: SUDO_PASSWORD
        sync: false
    healthCheckPath: /
    autoDeploy: true
